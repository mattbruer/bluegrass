let initialState = {
  title: 'YO!',
  steps: [[12, 13, 14, 15, 13, 12]],
};

const songReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default songReducer;
